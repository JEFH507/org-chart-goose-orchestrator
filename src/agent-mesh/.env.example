# Agent Mesh MCP Server Configuration

# Controller API Configuration
CONTROLLER_URL=http://localhost:8088

# JWT Token (obtain from Keycloak client_credentials grant)
# Example: curl -X POST http://localhost:8080/realms/dev/protocol/openid-connect/token \
#          -d "client_id=goose-controller" \
#          -d "grant_type=client_credentials" \
#          -d "client_secret=<secret>" | jq -r '.access_token'
MESH_JWT_TOKEN=

# Retry Configuration
MESH_RETRY_COUNT=3
MESH_TIMEOUT_SECS=30

# Optional: Override default tenant/session IDs
# MESH_TENANT_ID=
# MESH_SESSION_ID=
