#!/usr/bin/env bash
# Purpose: Probe health endpoints of infra containers once compose is running (Phase 0 placeholder)
# Scope: Placeholder only — assumes docker compose up is handled manually.
# Usage: ./scripts/dev/health.sh (future: read deploy/compose/.env.ce)

set -euo pipefail

echo "[Phase 0] health placeholder. Use deploy/compose/healthchecks/* scripts once available."
