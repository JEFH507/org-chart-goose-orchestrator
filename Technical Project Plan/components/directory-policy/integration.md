# Integration

- With identity: Use JWT claims; enrich with directory labels.
- With agent: Bootstrap config from signed profile; enforce allowlists locally.
- With controller: Check policy per route/send/approve.
