# Requirements

## Functional
- Strategy selection per task class; guard-first; fallback to local models when policy requires.

## Non-functional
- Performance: minimal added latency; caching allowed where safe.
